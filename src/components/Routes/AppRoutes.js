import React from "react";
import Form from "../Form";
import SignIn from "../SignIn";

const AppRoutes = [
  {
    path: "/form1",
    component: Form,
    exact: true,
    name: "form1",
  },
];

export default AppRoutes;
